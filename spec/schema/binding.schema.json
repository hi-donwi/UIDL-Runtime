{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://uidl.dev/schema/v1/binding.schema.json",
  "title": "UIDL Binding",
  "description": "A $bind path per spec/semantics/bindings.md: one of the five render scopes plus a dotted name. A string, not an object.",
  "type": "string",
  "pattern": "^(local|state|session|route|data)(\\.[^.]+)+$"
}